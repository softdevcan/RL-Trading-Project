# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment (but NOT our env/ trading environment folder!)
venv/

# Environment variables
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/tensorboard/
logs/*.txt
logs/

# Models and Data (RL Trading specific)
# NOTE: anchor to repo root with a leading slash. A bare `models/` also matches
# `prediction/models/` (a SOURCE package!) — that bug silently kept the Phase-2
# ensemble code out of git. Same applies to `results/` and `*.csv` below.
/models/
/results/
results/figures/
results/latex/
results/data/*.csv
results/data/*.json
data/raw/
data/processed/
data/*.csv
data/**/*.csv
# Stray CSVs at repo root, but never under prediction/ (that's source, not data).
/*.csv

# Tensorboard
runs/

# CatBoost training logs (regenerated on every fit — build artifact, not source)
catboost_info/

# Temporary files
*.tmp
*.bak
.~*

# Jupyter Notebooks
.ipynb_checkpoints/
*.ipynb

# Project specific
reviews/
PROJECT_STRUCTURE.md

# Kullanici calisma alanlari ve auth veritabani (Faz 7) — asla commit'lenmez
/workspaces/
data/auth/
*.db
*.db-wal
*.db-shm
.jwt_secret
